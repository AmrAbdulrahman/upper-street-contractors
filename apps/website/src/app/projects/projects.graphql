#import "../../components/ui/project-card/project-card.graphql"

query GetProjects($status: PublicationStatus = false) {
  projectCardCollection(limit: 50, status: $status) {
    items {
      ...ProjectCard
    }
  }
}
