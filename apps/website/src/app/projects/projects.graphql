#import "../../components/ui/project-card/project-card.graphql"

query GetProjects($status: PublicationStatus = PUBLISHED) {
  projectCards(pagination: { limit: 50 }, status: $status) {
    ...ProjectCard
  }
}
