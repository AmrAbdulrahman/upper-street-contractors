query GetPageMeta($key: String!, $status: CmsReadStatus = published, $includeUnpublished: Boolean) {
  pages(filters: { key: { eq: $key } }, pagination: { limit: 1 }, status: $status, includeUnpublished: $includeUnpublished) {
    meta {
      ...PageMetadata
    }
  }
}
