fragment HomeHeroSection on HomeHeaderSection {
  id
  type
  subtitle
  # aliased: HomeHeaderSection.title is `blocks` (JSON), which would clash with the
  # String `title` on other sections in the page `sections` union.
  headerTitle: title
  overline
  footer
  buttons {
    ...Button
  }
}
