# Verify app in browser

Read and follow `.agents/skills/verify-with-devtools/SKILL.md` immediately.

1. Ensure `npm run dev` is running (start if not).
2. Run the full verification checklist against `http://localhost:3000` (or the route the user specifies).
3. Report PASS/FAIL with console, network, and snapshot findings.
4. Fix clear failures before finishing unless the user asked for diagnosis only.

Use caveman mode (`.agents/skills/caveman/SKILL.md`) for all responses during this session.
